

<div class="box py-10">
    <div class="animation col-start-1 col-end-7text-4xl font-bold text-orange-100 ">
        Dairon Rodriguez Vega 🖖
    </div>
</div>

<style>
    .box {
  display: flex;
  align-items: center;
  justify-content: center;
}
    .animation {
        font: bold 200% Consolas, Monaco, monospace;
        border-right: .1em solid white;
        width: 24.2ch;
        white-space: nowrap;
        overflow: hidden;
        animation: typing 3s steps(24, end), blink-caret .16s step-end infinite alternate;
    }

    @keyframes typing {
        from {
            width: 0;
        }
    }

    @keyframes blink-caret {
        50% {
            border-color: transparent;
        }
    }

</style>